#################################################################
# This file generated by Chef for <%= node['fqdn'] %>
# Manual modifications will be overwritten by the next Chef run
#################################################################

server {
  listen 80;
#  listen 443;
#  ssl on;
#  ssl_certificate /etc/nginx/ssl/example.in.crt;
#  ssl_certificate_key /etc/nginx/ssl/example.in.key;

  location / {
    proxy_http_version 1.1;
    proxy_pass http://unix:/var/run/docker.sock;
  }
}
